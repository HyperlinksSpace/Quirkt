# Railway production — comma-separated browser origins allowed to call the API
QUIRKT_ALLOWED_ORIGINS=https://youruser.github.io,http://localhost:8080

# Local dev (Railway sets PORT automatically in production)
PORT=8000
